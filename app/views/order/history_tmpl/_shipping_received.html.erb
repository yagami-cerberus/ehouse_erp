<div class="order-history">
  <div class="order-summary">
    <div class="order-object">出貨單#<%=history.data['shipping_id'] unless history.data.nil? %> 內容已由 <%= history.user.nickname %> 簽收</div>
    <div class="order-at"><%= history.created_at %></div>
  </div>
  <div class="order-message">
    <%= history.message%>
  </div>
</div>